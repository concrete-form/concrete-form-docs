import Props, { Prop, Divider } from '../components/Props'

<Prop icon="cf.png" name="name" type="string" required>
  Name of the control, forwarded to the form library
</Prop>

<Prop icon="cf.png" name="fieldProps" type="object">
  Forwarded to the form library when registering the field <em>(if available)</em>
</Prop>

<Divider />